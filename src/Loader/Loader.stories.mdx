import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import Loader from './Loader';

<Meta title="Elements/Loader" component={Loader} />

# Loader

## Default

The `<Loader>` creates a simple CSS-only loader.

<Canvas>
  <Story name="default">
    <Loader />
  </Story>
</Canvas>

## Loader Props

<ArgsTable of={Loader} />
